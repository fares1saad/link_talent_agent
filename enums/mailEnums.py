from enum import Enum 

class MailEnums(Enum):

    REJECTION = "rejection"
    ACCEPTANCE = "acceptance"
    INTERVIEW_INVITE = "interview_invite"
    SCHEDULE_INTERVIEW = "schedule_interview"
    CUSTOM = "custom"
